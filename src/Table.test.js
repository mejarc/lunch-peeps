import React from 'react';
import Table from './Table';

describe('Table', () => {
  it('assembles a Table component with many Lunchers', () => {
    
  });
  it('assembles a Table with at least three Lunchers, and no more than five', () => {
    
  });
  it('assembles a Table with randomly chosen Lunchers each time', () => {
    
  });
});
